./calendar -h
```sh
calendar downloads and extrasts the list of fixtures.
Then creates, in google calendar, one event by fixture.

  -calendar string
        iCal identifier (default "primary")
  -fixtures int
        fixtures count (default 22)
  -league string
        league (default "N2")
  -team string
        team
  -url string
        official website of ffrs
```